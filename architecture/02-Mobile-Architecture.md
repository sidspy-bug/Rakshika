# Mobile Architecture

The mobile app is the primary client for end users and responders. It should be structured into screens, navigation, API clients, state management, and reusable UI components so feature teams can work independently.
